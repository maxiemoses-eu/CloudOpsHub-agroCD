apiVersion: v1
kind: Service
metadata:
  name: {{ include "das.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: {{ include "das.name" . }}
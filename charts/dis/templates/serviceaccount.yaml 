{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dis.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "dis.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "dis.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: 8001
      protocol: TCP
      name: http
  selector:
    app: {{ include "dis.name" . }}
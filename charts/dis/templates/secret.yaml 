apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.disSecret.name }}
type: Opaque
stringData:
  {{ .Values.disSecret.sourceApiKey }}: "external-api-key-value"
  {{ .Values.disSecret.jwtKey }}: "supersecretjwt"
